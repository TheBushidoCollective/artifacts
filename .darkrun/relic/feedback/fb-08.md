---
status: addressed
station: specify
severity: low
origin: adversarial_review
created_at: 2026-07-30T05:14:54.234799+00:00
---
COMPLETENESS round 3. **Not a blocker. Do not re-review on my account when this closes.** Every substantive gap from `fb-04` and `fb-07` is closed and held, the decomposition is sound, and my verdict is manufacture. This record exists because two of the four executable stub guards are set wrong and a message is easier to lose than a finding. All four items below are one-integer or one-line edits that move no material between units, so none of them can create the next defect the way the previous two rounds' fixes did.

## 1. `spec-relic-format`'s executable gate disagrees with its own body criterion

Body criterion 2 says `test "$(wc -w < docs/spec/format.md)" -ge 2000` and argues the number at length: "a compliant document lands somewhere between about 1600 and 2300 words. **2000 sits inside that band rather than below it.**"

The frontmatter gate says something else:

```yaml
- name: substance-floor
  command: test "$(wc -w < docs/spec/format.md)" -ge 1600
```

The gate is what runs. The body is prose. So format's real guard is **1600**, which is the exact bottom of its own stated band, which is precisely the defect I filed against viewer in `fb-07` and which viewer has now fixed. It arrived here as a body-and-gate mismatch rather than a wrong number, so reading either one alone misses it.

Verified against the other three, which all match exactly: service body 2200 / gate 2200, publish body 2200 / gate 2200, viewer body 2600 / gate 2600. Format is the only mismatch, and the sources-manifest gates match their body criteria in all four.

**Fix:** set the format gate to 2000.

## 2. `spec-service-surface`'s floor is genuinely too low, and `testability` was right where I was wrong

You applied my counting rule station-wide and got 2200 for service rather than `testability`'s 2850. The arithmetic was correct and my rule was wrong, so this is me conceding rather than reconciling.

My rule counted top-level decide-bullets. Service now has 29, giving a band of 1740 to 2465, and 2200 sits inside it. But service is the one unit carrying a large embedded enumeration: section 1 lists **12 app-server cases** and each one needs an individually reasoned status assignment. Those are decision units and my rule does not see them, because they are semicolons in a sentence rather than bullets.

Counting them: 29 + 12 = 41 decision units, band **2460 to 3485**. `testability`'s 2850 sits inside that. My 2200 sits 260 words below the bottom of it.

**Amended rule, which changes only service:** count top-level decide-bullets **plus** any explicitly enumerated case that requires its own decision. No other unit has an enumeration of that kind, so format, publish and viewer keep the numbers they have (with format corrected per item 1).

**Fix:** set service's floor to 2850, body and gate, and credit the calibration to `testability`.

While in there: service's criterion 2 calibration still says "a **14**-case status enumeration." It is 12 now, correctly, since "unknown container version (client-side, no status)" and "decryption failure (client-side)" were rescoped out. Both are owned elsewhere, which I verified: unknown container version in `spec-relic-format` section 3, decrypt-failed in `spec-viewer` section 6. The rescoping is right; only the number is stale.

## 3. One decision now has two owners: the mint response's checksum fields

The mint-response fix landed correctly on the whole, but left a seam. `spec-service-surface` section 2 now says "**You own the mint response's field set**" and names what viewer needs, including "possibly object length and CRC32C." `spec-viewer`'s out of scope agrees: "where you need a field on the mint response, state the need and let that sibling define it."

But viewer's Route-to-`shape` list still carries "whether the mint response carries object length and CRC32C." Under viewer's strengthened criterion 5, routing it is legitimate because it is named there. So viewer will write "routed to `shape`" for a field set service has just been told to define, and service's criterion 8 requires it to define that set. Two documents, one decision, no tiebreak.

**Fix:** delete that clause from viewer's Route-to-`shape` list. Viewer's section 6 bullet already had its routing instruction removed and now correctly just states the need, so this is the last stale reference.

## 4. `isError` was dropped when publish's error section split

The old `spec-publish-contract` section 1 carried a bullet named "**`isError` mapping**", which named the flag and gave the protocol-error examples (unknown tool, malformed request, server error). The split rewrote it into section 2's opening, which now says only "The spec splits protocol errors from tool execution errors, which carry actionable feedback the model self-corrects from." Grep confirms `isError` and "unknown tool" now appear in no unit.

`mcp-protocol-2026-07-28-constraints` states the mechanism: "`isError: true` marks an execution error the model should self-correct from." Nothing now mandates that publish say which mechanism carries a Relic failure, protocol-level or `isError` result.

Weight is low and I want to be honest about that: the answer is close to forced (all publish failures are execution errors, not protocol errors), section 2's opening gestures at the distinction, and section 1's "`outputSchema` versus error results" still mandates the adjacent shape decision. But naming the flag was worth one clause, and this station's whole premise is that near-forced is not the same as stated.

**Fix:** restore the flag name to section 2's opening sentence.

## What I re-checked and found held

**All of `fb-07` landed, and the group split is correct.** Publish section 2 now splits into Group A (app-server-originated, maps onto `service.md`, invents nothing) and Group B (no app-server status, publish owns the codes), with the grant-time and upload-time size refusals named separately as criterion 12 requires. Group A also gained two cases the old list lacked, malformed renderer class and egress kill switch. Both units state the boundary from both sides: service section 1's scope note tells it not to invent statuses for those legs and not to read their absence as an omission; publish's out of scope says "Group B codes are yours, because no app server participates in those paths." That is the tightest hand-off in the station.

**Service criterion 5 is fully strengthened**, both the "with what `shape` must choose" clause it had lost and the routing fence. Criterion 15 forces the takedown-disclosure decision, and section 1's bullet reinforces it inline: "It is not in this unit's Route-to-`shape` list and routing it fails criterion 5." The route item is correctly narrowed to status **values**, keeping the taxonomy's shape and distinguishability in the unit.

**Viewer's floor moved to 2600, body and gate**, with the calibration stated the way format's is. The size-cap contradiction is fixed by adding the cap to viewer's route list and rewording section 5 to say so explicitly. The Markdown-origin item is now "Decide here, do not route" with criterion 16. The dropped telemetry bullet is restored and, as you said, sits next to the bundled-SDK trap, which reads better than where it was, since both are pressures on the same prohibition.

**Nothing else was dropped in the split.** I grepped 26 load-bearing terms across all four units. Every one still resolves to exactly one owner: `resource_link`, `outputSchema`, `structuredContent`, `Idempotency-Key`, `ifGenerationMatch`, the nonce-reuse consequence, `Referrer-Policy`, `isSecureContext`, letterboxing, Open Graph, `keyid`, the zero-byte class hole, the reserved path set, convergent encryption, the copy-link affordance, the Defender test, link shorteners, Public Suffix List, and the transcript disclosure. The only casualty is `isError` in item 4.

**The read-first fix is now better than what I asked for.** All four units name the dead ref explicitly and say it exits 128. The three units with sibling inputs gained a stop-and-fetch instruction with an ordered fallback, and the fallback ref shape `darkrun/relic/units/specify/<unit>` matches the convention actually on the remote (`origin/darkrun/relic/units/frame/frame-artifact`), so it will resolve once the sibling lands.

**Structure still holds.** Four disjoint output pairs, no path collisions, every gate references only its own unit's outputs, and the wave order is format, then {service, viewer}, then publish.
