---
name: 'Write preconditions.md: the operating conditions and the go/no-go'
unit_type: doc
status: in_progress
depends_on:
- frame-artifact
worker: distiller
model: opus
station: frame
inputs:
- docs/frame.md
outputs:
- docs/preconditions.md
- docs/preconditions.sources.txt
branch: darkrun/relic/units/frame/frame-preconditions
started_at: 2026-07-30T03:36:42.096626+00:00
iterations:
- worker: framer
  started_at: 2026-07-30T03:36:42.096626+00:00
  completed_at: 2026-07-30T03:36:42.096626+00:00
  result: advance
  note: |-
    Framer beat complete. Committed `3066217` on `darkrun/relic/units/frame/frame-preconditions`, worktree clean. **3492 words, 19 sources, all four gates run for real and recorded with the nonce, all exit 0.** Every URL was pre-screened with the identical gate command before being cited, so the manifest never held an unverified entry. Orphan check clean in both directions, and the manifest was generated by extraction from the body rather than hand-maintained, so it cannot drift.

    # STRUCTURAL ISSUE, verified, not the framer's fault

    `docs/frame.md` was **not present in this unit's worktree**, despite being its declared input. The framer read it with `git show darkrun/relic/units/frame/frame-artifact:docs/frame.md`, read-only, no checkout or merge. Correct behavior given the contract, and it flagged the situation rather than working around it silently.

    I verified independently: `git cat-file -e darkrun/relic/frame:docs/frame.md` fails. The station branch holds only `frame-artifact.md` and `frame-preconditions.md`. **`frame-artifact`'s output never landed on the station branch.** Being repaired through the engine, not by hand. Noted here so the challenger and distiller know the two units' outputs are on branches that have not met, and so the checkpoint carries it.

    # The unobservable-quantity sweep, and it worked

    The framer ran it properly and it changed the document. Thirteen conditions are fully observable from operator-owned records (intake fetch, takedown timestamps, registrar and DNS, Search Console status, mint log, server request log, GCS checksum, live-origin fetch, limiter test). **Eleven are only partially observable, and each states the unobservable half in the same breath**, which is exactly criterion 9's requirement. The sharpest:

    - **Hard size cap:** enforcement is structural, but the **rejection rate is invisible to the app server**, because publish goes direct to GCS and the server is not in that path.
    - **Per-IP publish quota:** exactly observable as mints-per-address, but **the IP is not the actor** (shared CI egress, corporate NAT, cheap rotation), and with no accounts a per-user quota does not exist.
    - **Per-object download cap:** **a mint is not a download.** One mint can serve several fetches; many mints are never fetched. Link scanners (Slack unfurl, Safe Links, AV gateways) consume the cap indistinguishably, because separating them needs a viewing-origin script, which is forbidden. User-Agent is client-declared, so it is a hint and not a discriminator.
    - **Per-IP download rate limit:** the server rate-limits **mints, not bytes**. Once minted, transfer runs client-to-GCS outside the server's path, so **this control does not bound egress at all.**
    - **Egress spend kill switch:** billing export is not real-time, so the switch trips **after** the spend and a sharp spike can overrun before the number exists.
    - **Ciphertext-hash blocklist:** detects identical byte streams only. **Re-encryption under a fresh key sails through, so a zero hit count is not evidence nothing was re-uploaded.** Detect-and-delete, not refuse-at-the-door.
    - **Retention window:** true only after every sink is enumerated (application records, LB logs, GCS access logs are separate stores). Checking one and publishing the number is how the claim becomes false.
    - **Abuse alias delivery:** a synthetic probe proves delivery from where the probe sends, not that an arbitrary sender's report arrives. Failure mode is silence.
    - **TTL:** app-layer refusal is exact; the GCS lifecycle half is day-granular, UTC-midnight-rounded, up to 24h to apply. Lifecycle is storage hygiene, the refusal is the abuse-bounding control.
    - **noindex/robots.txt:** stops indexing, not crawling or classification. Immich's previews were found because URLs were posted to GitHub.

    **The load-bearing admission, stated twice in the document:** SLA and time-to-takedown measure responsiveness on reports **received**, never coverage. The operator cannot inspect content, so unreported abuse is invisible by construction and there is no denominator. A month of zero reports is either a clean service or a dead intake, and from the inside they are identical.

    Also handled per the sibling's lesson: every mechanism says plainly whether it is in or out. Ciphertext-hash blocklist is in, budgeted as a speed bump. PSL registration is discussed and explicitly **not** adopted as a guaranteed control; it sits in section 5 as unresolved.

    The framer's first draft had **zero authorial contractions**, the identical defect that hit `frame-artifact`, and it caught this by grep before committing rather than shipping it. Final: 36 authorial contractions, 14 flat forms deliberately retained on load-bearing claims. Dashes verified by `grep -nP` over a unicode dash range across both files and the commit message: none.

    ---

    # CHALLENGER: two the framer named, plus where to hunt

    **1. The cost precondition is the softest thing in the document, by its own admission.** It cites $0.12/GB and argues the kill switch is therefore v1, but never states a ceiling and never works the arithmetic that would make a ceiling meaningful: how much egress one hostile download loop actually produces against the size cap and the per-object cap. That leaves the one recommendation-shaped sentence in a document whose entire job is binary conditions. The framer leans toward saying the ceiling is `shape`'s to set and naming what inputs `shape` needs. Judge whether that is the right call or whether the arithmetic belongs here.

    **2. "Signed into the upload grant" may be stronger than the unnamed mechanism supports.** The document says the size cap is signed into the grant so an oversized object "cannot come into existence." Grounded in the knowledge base (signed policy documents declare max file size, or `X-Upload-Content-Length` on a resumable session), but it deliberately names neither, because picking one is `shape`'s call. Decide whether the claim needs weakening or whether the two-options-both-work argument holds.

    **Where to hunt beyond those.** The framer's sweep was genuinely good, so do not re-run it identically and declare it clean. Attack differently:
    - **Verify a sample of the "fully observable" thirteen**, not the eleven it already flagged. It marked those clean, and its own attention was on the partial ones. Instance 4 in `frame-artifact` was found in a condition everyone had already read past.
    - **Fidelity to `docs/frame.md`.** It is locked. Check nothing here redefines the problem, user, wedge, success metric, or telemetry decision, and that section 4's telemetry-compatibility statement matches what `frame.md` actually specifies rather than what this document assumes it specifies. Read `frame.md` via `git show darkrun/relic/units/frame/frame-artifact:docs/frame.md`.
    - **Scope.** Does anything here stray into `shape` (naming a wire format, a framework, an endpoint) or restate what `frame.md` owns?
    - **3492 words against a 1000 floor.** Same question the sibling faced. Measure per section before concluding anything, and cut prose rather than mandated content.
    - **The go/no-go must be genuinely binary.** If any part reads as a recommendation, that is a defect in this document specifically.

    Re-run all four gates after any edit and re-record with the nonce. Verify criteria 5 through 9 by reading. Grep for dashes, do not eyeball. Commit in the unit worktree, never the station branch.
- worker: challenger
  started_at: 2026-07-30T03:51:13.044835+00:00
  completed_at: 2026-07-30T03:51:13.044835+00:00
  result: advance
  note: |-
    Challenger beat complete. Committed `ad3c612`. Worktree clean. **4507 words (was 3492), 22 sources (was 19), all four gates re-run and re-recorded with the nonce, all exit 0.** The three new URLs were pre-screened with the identical gate command AND each page's text read to confirm it actually supports the claim. Manifest regenerated by extraction, orphan check clean both directions via `comm -3`.

    # Four HIGH findings

    **1. The ciphertext-hash blocklist named a number the system cannot produce, and contradicted itself doing it.** Its mechanism clause said the checksum is "compared on publish, with refusals counted in the server's own log." Its limit clause, two sentences later, said the comparison runs against the stored object, "detect and delete rather than refuse on upload." Both cannot be true, and **if there is no refusal there is no refusal count.** "Compared on publish" is also impossible, since at mint time the object does not exist. This is the project's signature defect **inside a mechanism clause, in one of the eleven the framer had already flagged**, which is exactly why it survived. Fixed: the number is deletions triggered by a match; refusals are not a quantity this control has. Also added that the CRC32C GCS records is **32 bits**, trivially collided on purpose and colliding by accident at corpus scale, so it identifies an object and cannot police one, and that detect-and-delete means every blocklisted payload is servable between upload and detection, making the scan interval part of the control.

    **2. GCS soft delete is ON BY DEFAULT and silently falsified three claims.** Verified from Google's docs: soft delete is enabled by default on all buckets with a seven-day retention, soft-deleted objects cannot be read or modified, and Object Lifecycle Management is compatible with it but does not affect soft-deleted objects. So the takedown primitive, "lifecycle then reaps the bytes," and the published retention window were all overstated. **Deletion stops serving immediately, which is the half that answers an abuse notice, and does not erase.** It is a bucket-creation setting, so it is a precondition, and it is the one you get by not deciding. Recorded separately as durable knowledge.

    **3. Search Console verification is not a one-time gate.** Google: verification "lasts as long as Search Console can confirm the presence and validity of your verification token," it re-checks periodically, and permissions expire after a grace period if it cannot be confirmed. Losing it silently returns the operator to exactly the flagged-and-blind state section 2 exists to prevent. Same source: if all verified owners lose access, everyone does, making section 1's named human a single point of failure. Fixed, plus a second verified owner now required. **This was in the thirteen the framer marked fully observable.** Same treatment applied to "domains exist and are separate, checked once": registration has an expiry, so the standing observable is registrar expiry and auto-renew, and **a lapsed relic domain is worse than a flagged one, because someone else can buy it and inherit every link ever shared.**

    **4. The document asserted an architecture `frame.md` does not lock, and its sharpest limits rested on it.** "Publish uploads go direct to GCS and the app server isn't in that path by design" was stated as settled fact. `frame.md` says only "uploads only ciphertext to a service backed by Google Cloud Storage." It locks the **download** leg (its telemetry counts opens at signed-URL mint) and says nothing about the publish leg. Four of section 3's best limits were resting on an unstated assumption. Fixed by making it a section 4 precondition (the ciphertext never transits the app server, which is the part of the zero-knowledge claim that is structural rather than intent-based), naming its observability cost, and routing deviation to `frame` as drift rather than letting `shape` settle it quietly.

    # Five more

    **5 (MED-HIGH).** TTL ceiling is not what the document said: a URL minted a second inside the ceiling stays valid for its own lifetime, so the real bound on circulation is **TTL plus signed-URL validity**. Fixed, and it surfaced the signed-URL window as a named `shape` input that appears nowhere else in the run.
    **6 (MED).** `/abuse` returning 200 proves the page serves, not that the intake works. A webform posts into a mailbox or queue and fails there identically to the alias. Fixed in both places.
    **7 (MED).** The SLA drops unseen reports from numerator *and* denominator, so it reads better for having lost one. The coverage paragraph covered unreported abuse but not unreceived reports. Fixed.
    **8 (MED).** The robots/noindex check was scoped to "the live origin," but there are two registrable domains and per-ID relic paths, and the header is per-response, so asserting it on the apex proves nothing about `/{id}`. Fixed.
    **9 (LOW).** Size-cap compliance "checked by listing object sizes" evidences current compliance only, since a mandatory TTL is reaping continuously. Fixed.

    # Verdicts on the framer's two self-flagged items

    **The cost precondition: the framer's lean was half right.** The ceiling's value is `shape`'s, but **the arithmetic belongs here**, because it derives entirely from controls this document already mandates and it is what turns the softest paragraph into the binary condition the section was missing. Added: worst-case egress per relic is size cap times per-object cap times fetches one minted URL can serve inside its validity, times $0.12/GB. Then the part no control bounds: publishing is unauthenticated and nothing caps relic count, so a per-relic worst case does not produce a global one, and the per-IP publish quota slows an attacker by exactly what rotating an address costs. Restated as binary: **a global spend ceiling exists and is wired to disable minting before the first deploy, or Relic does not deploy.** Treated as a deployment blocker in the same class as the domain dependency, so section 1's gate remains the only "do not build" branch.

    **"Signed into the upload grant" needed a qualifier, not weakening, and not the one the framer was considering.** The two-options-both-work argument holds for the two options it had in mind. The problem is a third and more likely one: **a plain signed PUT does not bound body length (`Content-Length` is ignored), so picking the convenient grant silently turns the cap into a client-side suggestion, present in the code and absent on the wire.** Changed to state the "cannot come into existence" claim holds only for a grant carrying a signed size constraint, that which shape gets used is `shape`'s call, and that the chosen one enforcing size is this document's condition.

    # Verified clean (evidence, not silence)

    Dash sweep by `grep -nP` over U+2010 to U+2015, U+2212, U+FE58, U+FE63, U+FF0D across both files and the commit message: none. **Criterion 8 verified verbatim against `frame.md`:** all three telemetry items match exactly, and `frame.md`'s own heading over that list is "All server side. None of it needs a script on the viewing origin." No drift between what section 4 claims and what `frame.md` says. Frame fidelity generally: every reference checked against source, nothing redefines the problem, user, wedge, metric, or telemetry decision. Scope: no wire format, language, framework, topology, schema, or ID format chosen. **Section 5 checked item by item against the knowledge base, all five genuinely unresolved**, nothing parked there that the base answers. Voice: 50 authorial contractions, 32 flat forms retained. The eight remaining section 3 partials re-read and correct as written.

    ---

    # DISTILLER: two open calls

    **1. The section 4 ciphertext-path bullet is a structural addition.** The contract enumerates four bullets for section 4 and the challenger added a fifth. Its reasoning: it makes explicit an architectural condition the document already asserted twice as fact and that criterion 9 depends on, so it is not a new requirement. That reasoning is sound to me, but it is the one place structure changed rather than content, so it is yours to keep or fold. If you keep it, confirm it reads as a precondition inherited by `shape` rather than as `frame`-level redefinition, since finding 4 turns on exactly that boundary.

    **2. Length: 4507 words, up 29 percent, against a 1000 floor.** Per section: preamble 176, s1 829, s2 640, **s3 1981**, s4 504, s5 289, closing 50. Section 3 is 1981 for twelve controls, about 165 each for condition plus mechanism plus limit. The challenger cut ~110 words of prose in the same pass and says every word added is a mechanism clause, a limit clause, or a binary condition. It argues the only remaining place to cut is evidence and argues against that. **Measure independently before accepting, as you did on the sibling, where you found the challenger's own beat count understated its case.** The sibling's length held up on measurement; this one is 84 percent longer than that, so it deserves the same scrutiny rather than the same answer.

    **Your job beyond those.** Verify the four HIGH fixes rather than assuming them, especially finding 4, since it changes what this document claims the architecture guarantees and routes deviation to `frame` as drift. Confirm the soft-delete claims against the cited Google docs rather than trusting the summary. Sample the framer's "fully observable" set again: the challenger found two defects there after the framer marked it clean, so a third pass is warranted precisely because two passes already paid. Re-run all four gates and re-record with the nonce. Verify criteria 5 through 9 by reading. Grep for dashes. `docs/frame.md` is still not in the worktree; read it via `git show darkrun/relic/units/frame/frame-artifact:docs/frame.md`, read-only.
reviews:
  feasibility:
    at: 2026-07-30T02:47:23.386041+00:00
  value:
    at: 2026-07-30T02:45:12.239582+00:00
quality_gates:
- name: artifact-exists
  command: test -f docs/preconditions.md
- name: substance-floor
  command: test "$(wc -w < docs/preconditions.md)" -ge 1000
- name: sources-manifest-populated
  command: bash -c 'set -eu; n=$(grep -c . docs/preconditions.sources.txt); test "$n" -ge 5'
- name: every-cited-url-resolves
  command: bash -c 'set -eu; while IFS= read -r u || [ -n "$u" ]; do [ -n "$u" ] || continue; curl -sfL --max-time 25 --retry 2 -A "Mozilla/5.0 (relic-link-check)" -o /dev/null "$u"; done < docs/preconditions.sources.txt'
gate_results:
- name: artifact-exists
  status: pass
  at: 2026-07-30T03:48:15.147758+00:00
  attempts: 2
  detail: 'challenger beat: test -f docs/preconditions.md -> exit 0'
- name: substance-floor
  status: pass
  at: 2026-07-30T03:48:17.472742+00:00
  attempts: 2
  detail: 'challenger beat: exit 0; wc -w = 4507 (was 3492), floor 1000. Growth is mechanism/limit beats added under criterion 9 plus one binary condition; ~110 words of prose cut in the same pass.'
- name: sources-manifest-populated
  status: pass
  at: 2026-07-30T03:48:25.043309+00:00
  attempts: 2
  detail: 'challenger beat: exit 0; 22 non-empty URL lines (was 19), floor 5; trailing newline confirmed via xxd (0a). Manifest regenerated by extraction from the body, so it cannot drift.'
- name: every-cited-url-resolves
  status: pass
  at: 2026-07-30T03:48:34.626161+00:00
  attempts: 2
  detail: 'challenger beat: exit 0; all 22 URLs resolved via curl -sfL. The 3 new ones (soft-delete, data-validation, webmasters/9008080) were pre-screened with the identical command before being cited, and I read each page''s text to confirm it supports the claim. Orphan check clean both directions.'
---

# Goal

Write `docs/preconditions.md`: the conditions that must hold for Relic to be buildable and operable at all, and the explicit go/no-go that follows from them. This is the honest gate on the entire run. `harden` inherits it most directly, but every station is bound by it.

Also write `docs/preconditions.sources.txt`, a citation manifest: one URL per line, no other text, listing every external source this document relies on. End the file with a trailing newline.

**Read `darkrun_knowledge_list` first, in full**, and read `docs/frame.md`, which this unit depends on and which is already locked. You have no other context. Write the preconditions against the frame that unit settled; do not redefine the problem, the user, the wedge, the success metric, or the telemetry decision.

**A warning about the citation manifest.** Some knowledge topics contain illustrative URL-shape examples rather than citations, such as `https://file.kiwi/abcdef12#secretKey` and `https://wormhole.app/{roomId}#{mainSecretKey}`. Those are templates, not sources. They 404 and will fail the gate. Only real, resolvable sources go in the manifest.

# MANDATORY: the unobservable-quantity sweep

**Read `unobservable-quantities-are-this-projects-failure-mode` before you write, and run its sweep on your finished draft before you commit.**

This project's specific, recurring authoring defect is claiming a number the system cannot actually produce. It was caught four times in the sibling unit `frame-artifact`, by four different readers, three of them after an explicit sweep for exactly this. Each instance was subtler than the last. The dangerous ones are *partially* observable: a wholly fabricated metric gets caught, a half-true one does not.

Your document is dense with checkable conditions, so it is more exposed to this defect than any other unit in the run. **For every condition you state as checkable, name the exact mechanism that produces the number, and confirm the locked architecture permits it.** Where a quantity is partly observable and partly not, say which half is not, in the same breath as the claim.

Two live examples from `frame-artifact` to calibrate on. A claim that the client-type split is computable from publishing client name is half-wrong, because a Claude Code run inside a GitHub Action reports the same client name as an interactive one. A claim that mail-gateway blocklisting is checkable on a schedule is half-wrong, because a block inside a single company's tenant is invisible from outside and surfaces as a recipient reporting a dead link. Both were fixed by stating the limit alongside the claim, not by removing the condition.

This applies with particular force to your section 3, where every control is stated as a checkable condition.

# Why this document exists separately

Relic hosts content the operator is structurally unable to inspect, on a public unauthenticated endpoint. That is the exact shape that killed Firefox Send and AnonFiles. The frame says what Relic is for. This document says what has to be true for it to survive contact with the real world, and states plainly when the answer is "do not build."

# Required content

## 1. The abuse-operations commitment (the go/no-go)
State it as a binary condition, not a recommendation. Google Cloud's AUP makes this contractual, not optional: for organizations hosting third-party content, Google requires a published prohibited-content policy, a reporting intake process (a webform or email alias), prompt review and removal, and log monitoring. Failure to respond to an abuse notice risks **project-level** suspension, not just bucket removal.

Say what that obligates: a named human who answers the abuse address, a takedown path documented publicly at a stable URL, and an SLA. Then state the go/no-go: if that commitment is not made, the correct decision is not to build. Mozilla had a legal team and shut Send down rather than carry this.

## 2. The domain preconditions
Two registrable domains distinct from `thebushido.co` must be acquired before anything deploys: one for the service (API and PWA), one for the sandbox origin that renders untrusted HTML. `thebushido.co` carries marketing and email and never hosts user-generated content.

Ground this in the Immich precedent (October 2025): Google flagged all of `*.immich.cloud`, including internal-only services, triggered by per-PR preview environments, and it recurred after a successful appeal. Their fix was a separate registrable domain. Note that Microsoft's Tenant Allow/Block List blocks a URL domain and all subdomains by default.

State the Search Console precondition: every domain verified **before** launch, because the Security Issues report is the only place a listing's triggering URLs are visible. Unverified means flagged and blind.

Mark domain acquisition as an **external dependency requiring operator action**. Name it as a blocker on deployment, not on design.

## 3. The v1 control set
List the controls that must ship in the first release rather than being deferred. Each stated as a checkable condition, not an aspiration, and each subject to the sweep above. Drawn from `abuse-liability-of-hosting-uninspectable-content`: mandatory non-configurable TTL, hard size cap, per-IP publish quota, per-object download cap, per-IP download rate limit, global egress spend kill switch, delete-by-ID that works without the decryption secret, ciphertext-hash blocklist, upload IP plus timestamp retention with a published window, abuse reporting reachable from every relic page and a stable `/abuse` URL and a published email alias, and `robots.txt` disallow plus `X-Robots-Tag: noindex`.

Explain briefly why each is load-bearing rather than listing them bare. Two points worth making explicitly: mandatory short TTL is the single highest-leverage control because it bounds how long abuse circulates, and delete-by-ID works precisely because the relic ID is not secret, only the key is.

State the cost precondition too: GCS internet egress runs $0.12/GB for the first TB, and an unauthenticated endpoint has no identity to throttle against, so the spend kill switch is a v1 requirement.

## 4. The security preconditions that constrain design
Short, so `shape` and `build` inherit them as constraints rather than discovering them late:
- Untrusted content renders on a **separate origin**, never the origin holding the fragment secret. Origin isolation is the first layer; sanitization is the second.
- Never set both `allow-scripts` and `allow-same-origin` on the render iframe.
- The viewing origin carries no third-party scripts, no analytics, and no error reporting, because any same-origin script can read `location.hash`. Note that `docs/frame.md` specifies the run's only telemetry, and that all of it is collected server-side at publish and at signed-URL mint, never by a script on the viewing origin. These two requirements are compatible; say so explicitly so nobody later reads the telemetry decision as license to add a script here.
- Rate limiting returns `429`, never `401` or `403`, or Claude Code will flag the server as needing OAuth sign-in against an authorization server that does not exist.

## 5. What is honestly unresolved
List the open questions that remain genuinely unsettled, so nobody downstream mistakes silence for a decision. At minimum: whether a Safe Browsing listing on a subdomain degrades the parent's Gmail sender reputation (industry consensus says yes, Google publishes nothing); how Proofpoint's URI blocklist treats host-to-parent relationships (unpublished); Google's actual timeline between abuse notification and project suspension (documented only as "timely"); and the legal exposure of the plausible-deniability posture, which is a lawyer question, not a research question.

Be honest that these are unknown. Do not paper over them.

# Style

Write as Jason Waldrip would: direct, dry, confident, **contractions**, brevity, authority through specificity. No corporate-speak, no hedging, no stock AI phrasing. **Never use an em-dash or an en-dash.** Rewrite with a comma, a colon, parentheses, or two sentences. No emoji.

On contractions specifically: the sibling unit's first draft contained **zero** contractions across 2542 words, every apostrophe a possessive, and that was flagged as a high-severity voice defect and one of the strongest AI tells there is. Use them naturally. Do not use them mechanically either: keep the flat form where a human would say "is not" for emphasis, particularly on load-bearing claims.

# Completion criteria

1. `docs/preconditions.md` exists → `test -f docs/preconditions.md` exits 0.
2. It is substantive and complete, not a subset → `test "$(wc -w < docs/preconditions.md)" -ge 1000` exits 0. This floor is a completeness signal, not a target. It sits below the natural length of a compliant document (five sections, one enumerating roughly twelve controls with a rationale line each) so it cannot block correct work, but high enough that a document missing a whole section fails it. Do not pad to reach it, and do not treat clearing it as evidence of completeness; criteria 5 through 9 are what actually check that.
3. `docs/preconditions.sources.txt` lists at least five sources, one URL per line, nothing else, ending with a trailing newline → `bash -c 'set -eu; n=$(grep -c . docs/preconditions.sources.txt); test "$n" -ge 5'` exits 0.
4. Every listed source resolves over the network → `bash -c 'set -eu; while IFS= read -r u || [ -n "$u" ]; do [ -n "$u" ] || continue; curl -sfL --max-time 25 --retry 2 -A "Mozilla/5.0 (relic-link-check)" -o /dev/null "$u"; done < docs/preconditions.sources.txt'` exits 0. **Do not invent citations.** Every URL comes from the recorded knowledge topics or is one you verified yourself. Run the orphan check in both directions: every manifest URL cited in the body, and every body URL present in the manifest.
5. The document states the abuse-operations go/no-go as a binary condition with an explicit "do not build" branch.
6. The document names domain acquisition as an external dependency blocking deployment.
7. The document contains a section of honestly unresolved questions.
8. Section 4 states explicitly that the run's telemetry is collected server-side only and does not conflict with the no-script-on-the-viewing-origin rule.
9. **Every checkable condition in the document names the mechanism that produces its number, and any condition that is only partially observable says so in the same breath.** This is the mandatory sweep above, and it is the criterion most likely to be violated. No gate can check it; it is verified by reading.

# Files touched

- `docs/preconditions.md` (create)
- `docs/preconditions.sources.txt` (create)

# Out of scope

- Restating or redefining the problem, user, value, success metric, or telemetry decision. Those are locked in `docs/frame.md`.
- Choosing the server language, framework, hosting topology, or encryption wire format. That is `shape`.
- Endpoint design, schemas, relic ID format.
- Actually buying domains, configuring GCP, or writing any code, config, or infrastructure.
- Designing the abuse-report UI. State the requirement, not the interface.
